

function LoginPage() {
  const handleClick = () => {};
  return (
    <div>
      <h2>Login Page</h2>
      <button onClick={handleClick}>Login</button>
    </div>
  );
}

export default LoginPage;
